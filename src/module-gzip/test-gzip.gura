#!/usr/bin/env gurax
//==============================================================================
// test-gzip.gura
//==============================================================================
import(util.tester) {*}
import(gzip)
import(hash)

TestCase('Basic test') {
	/*
	gzip.Reader('test.gz') {|s|
		Print(s.ReadLines())
	}
	*/
	gzip.Writer('test2.gz') {|s|
		s.Print(ReadLines('Util.cpp'))
		//s.Write(b'fjsdkfjladksjfkjadslkfjksadjfldjsfkjadsfljdaskfjdas')
		//s.Flush()
	}
}
