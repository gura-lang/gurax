#!/usr/bin/env gurax
//==============================================================================
// test-gzip.gura
//==============================================================================
import(util.tester) {*}
import(gzip)

TestCase('Basic test') {
	/*
	gzip.Reader('test.gz') {|s|
		Print(s.ReadLines())
	}
	*/
	gzip.Writer('test2.gz') {|s|
		s.Print(ReadLines('ZLibHelper.h'))
		s.Flush()
	}
}
