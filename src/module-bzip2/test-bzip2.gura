#!/usr/bin/env gurax
//==============================================================================
// test-bzip2.gura
//==============================================================================
import(util.tester) {*}
import(bzip2)

src = B''
src.p.Put(`uint8, (0..255).Cycle(4096))

TestCase('Basic test') {
	buff = B''
	bzip2.Writer(buff).Write(src)
	buff.Dump()
	bzip2.Reader(buff).Read().Dump()
	/*
	bzip2.Reader('test2.bz2') {|s|
		Print(s.ReadLines())
	}
	*/
	/*
	bzip2.Writer('test1.bz2') {|s|
		s.Print(ReadLines('BZLibHelper.h'))
	}
	*/
}
