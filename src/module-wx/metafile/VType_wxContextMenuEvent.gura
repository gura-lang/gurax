//==============================================================================
// VType_wxContextMenuEvent.gura
//==============================================================================
ContextMenuEvent = Class(CommandEvent) {
	@include['wx/event.h']
	GetPosition() as Point
	SetPosition(point as const_Point_r) as void
} %% R'''
# Overview

# Predefined Variable

${help.ComposePropertyHelp(wx.ContextMenuEvent, `en)}

# Operator

# Cast Operation

${help.ComposeConstructorHelp(wx.ContextMenuEvent, `en)}

${help.ComposeMethodHelp(wx.ContextMenuEvent, `en)}
''' %% R'''
# 概要

# 定数

${help.ComposePropertyHelp(wx.ContextMenuEvent, `ja)}

# オペレータ

# キャスト

${help.ComposeConstructorHelp(wx.ContextMenuEvent, `ja)}

${help.ComposeMethodHelp(wx.ContextMenuEvent, `ja)}
'''ja

@event {
	EVT_CONTEXT_MENU >> ContextMenuEvent
}
