//==============================================================================
// VType_wxSizeEvent.gura
//==============================================================================
SizeEvent = Class(Event) {
	GetSize() as Size
	SetSize(size as Size) as void
	GetRect() as Rect
	SetRect(rect as Rect) as void
}

@event[
	EVT_SIZE >> wxSizeEvent
]
