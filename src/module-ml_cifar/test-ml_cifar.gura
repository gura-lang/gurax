#!/usr/bin/env gurax
//==============================================================================
// test-ml_cifar.gura
//==============================================================================
import(util.tester) {*}
import(tar)
import(png)
//cifar = import(ml.cifar10)
cifar = import(ml.cifar100)

TestCase('Basic test') {
	db = cifar.Database('.')
	Println(db.train)
	Println(db.test)
	/*
	db.train.Each(`float).Head(3) {|sample, idx|
		Println(db.labelNames[sample.label])
		cifar.PrintImage(sample.input, guideFlag = true)
		sample.image.Resize(100, nil).Write(Format('a%d.png', idx), `png)
	}
	*/
	db.train.EachBatch(`float, 16).Head(1) {|sample, idx|
		repeat (sample.input.shape[0]) {|i|
			Println(db.labelNames[sample.labels[i]])
			cifar.PrintImage(sample.input[i], guideFlag = true)
		}
		sample.image.Resize(100, nil).Write(Format('a%d.png', idx), `png)
	}
}
