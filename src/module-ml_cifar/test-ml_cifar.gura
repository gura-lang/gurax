#!/usr/bin/env gurax
//==============================================================================
// test-ml_cifar.gura
//==============================================================================
import(util.tester) {*}
import(ml.cifar)
import(tar)

TestCase('Basic test') {
	//cifar = ml.cifar.Cifar10('cifar-10-binary.tar.gz', `test)
	cifar = ml.cifar.Cifar100('cifar-100-binary.tar.gz', `train)
	Println(cifar)
}
