#!/usr/bin/env gurax
//==============================================================================
// test-ml_cifar.gura
//==============================================================================
import(util.tester) {*}
import(ml.cifar100)
import(tar)

TestCase('Basic test') {
	//cifar = ml.cifar.Cifar10('.', `test)
	cifar = ml.cifar100.Database('.', `train)
	Println(cifar)
}
