#!/usr/bin/env gurax

Radians(degree:Number):map = degree * math.pi / 180

Cross(a[]:Number, b[]:Number) = {
	[a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0]]
}

Normalize(a[]:Number) = a / math.Sqrt((a * a).Sum())
