#!/usr/bin/env gurax
//==============================================================================
// test-bmp.gura
//==============================================================================
import(util.tester) {*}
//import(bmp)

TestCase('Basic test') {
	img = Image('cat.bmp', `rgb)
	//Println(img)
	//Printf('%dx%d\n', img.width, img.height)
	img.Write('t.bmp')
}
