#!/usr/bin/env gurax
//==============================================================================
// test-ml_mnist.gura
//==============================================================================
import(util.tester) {*}
import(ml.mnist)

db = ml.mnist.Database('../../sample/resource/mnist')

TestCase('Basic test') {
	Printf('nRows=%d, nCols=%d, nLabels=%d\n', db.nRows, db.nCols, db.nLabels)
	Printf('%s\n', db.train.imageSet)
	Printf('%s\n', db.train.labelSet)
	Printf('%s\n', db.test.imageSet)
	Printf('%s\n', db.test.labelSet)
}

TestCase('ml.mnist.DBPair#Print()') {
	db.train.Print()
}
