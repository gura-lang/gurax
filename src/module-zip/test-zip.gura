#!/usr/bin/env gurax
//==============================================================================
// test-zip.gura
//==============================================================================
import(util.tester) {*}
import(zip)

TestCase('Basic test') {
	d = zip.Open('test.zip')
	stats = path.Walk(d):stat:list
	Printf('%-32s %8d %8d\n', stats:*fileName, stats:*size, stats:*sizeCompressed)
}
