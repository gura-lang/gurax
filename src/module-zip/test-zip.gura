#!/usr/bin/env gurax
//==============================================================================
// test-zip.gura
//==============================================================================
import(util.tester) {*}
import(zip)

TestCase('Basic test') {
	d = zip.Open('test.zip')
	stats = path.Walk(d):stat
	Printf('%-32s %6d %6d\n', stats:*fileName, stats:*size, stats:*sizeCompressed)
}
