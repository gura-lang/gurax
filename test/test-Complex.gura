#!/usr/bin/env gurax
import(util.tester) {*}

TestCase('Property') {
	Tester(`Complex.__PropHandler__(`real))
	Tester(`Complex.__PropHandler__(`imag))
}

TestCase('Complex#re and Complex#im') {
	a = 3j
	Tester(`a)
	Tester(`a.real)
	Tester(`a.imag)
}
