#!/usr/bin/env gurax
import(util.tester) {*}

TestCase('Property') {
	//Tester(`os.__GetProp__(`cin))
	//Tester(`os.__GetProp__(`cout))
	//Tester(`os.__GetProp__(`cerr))
}

TestCase('Streams') {
	Tester(`os.cin)
	Tester(`os.cout)
	Tester(`os.cerr)
	os.cout.Println('print message on os.cout')
}

TestCase('os.Redirect()') {
	os.Redirect(nil, nil) {
		Tester(`os.cin)
		Tester(`os.cout)
		Tester(`os.cerr)
	}
}
