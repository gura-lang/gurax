#!/usr/bin/env gurax
//Printf('%.3f\n', os.Clock())
f(x):map = { x * x }
//f(x) = {}
t = os.Clock {
	u = repeat (100000):iter {|i| f(i)}
	[u]
}
Println(t)

t = os.Clock {
	repeat (100000):list {|i| f(i)}
}
Println(t)

x = [0..(100000 - 1)]
t = os.Clock {
	f(x)
}
Println(t)
