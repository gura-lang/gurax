#!/usr/bin/env gurax
import(util.tester) {*}

TestCase('Property') {
	PrintPropSlots(Symbol)
}

TestCase('Operator ==') {
	x = `a
	Println(x.__vtype__)
	Tester(`(`a == `a))
	Tester(`(x == `a))
}

TestCase('Operator !=') {
	x = `a
	Println(x.__vtype__)
	Tester(`(`a != `a))
	Tester(`(x != `a))
}
