#!/usr/bin/env gurax
import(util.tester) {*}

TestCase('Operator ==') {
	x = `a
	Println(x.vtype)
	Tester(`(`a == `a))
	Tester(`(x == `a))
}

TestCase('Operator !=') {
	x = `a
	Println(x.vtype)
	Tester(`(`a != `a))
	Tester(`(x != `a))
}
