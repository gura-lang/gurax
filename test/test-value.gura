#!/usr/bin/env gurax
import(util.tester) {*}

TestCase('Comparison of VTypes') {
	Tester(`(String == String))
	Tester(`(String != String))
	Tester(`(String == Number))
	Tester(`(String != Number))
}

/*
TestCase('Create Iterator') {
	Tester(`(3))
	Tester(`())
	Tester(`(3,))
	Tester(`(3, 1, 4, 1, 5, 9, 2, 6, 5, 3))
}
*/
